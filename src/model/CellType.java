package model;

// Dit is een Enum: een vaste lijst met unieke keuzemogelijkheden (opties)
public enum CellType {
    // De optie voor de originele Conway-cellen
    CONWAY,

    // De optie voor de alternatieve cellen
    ALTERNATIVE
}